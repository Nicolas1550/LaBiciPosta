/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    compiler: {
      styledComponents: true, // Activa la compatibilidad con styled-components
    },
  };
  
  export default nextConfig;
  